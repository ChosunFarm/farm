  datasource:
    url: jdbc:mysql://127.0.0.1:3306/farmshop?useSSL=false&serverTimezone=Asia/Seoul
    username: AHYUN 
    password: ChosunFarm@@2025!
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    cache: false

  file:
    upload:
      directory: ./uploads/images   # 업로드 루트

  web:
    resources:
      static-locations:
        - classpath:/static/        # 기존 스태틱
        - file:./uploads/
      cache:
        period: 0

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
    org.thymeleaf: TRACE
    org.thymeleaf.parser: TRACE